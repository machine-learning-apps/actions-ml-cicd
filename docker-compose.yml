version: '3'
services:
  ml:
    network_mode: "host"
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/data
    environment:
      - DASK_SCHEDULER
    command: tail -f /dev/null 
