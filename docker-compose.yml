version: '3'
services:
  ml:
    container_name: actions-ml-cicd_ml_1
    image: hamelsmu/ml-ci-cd
    network_mode: "host"
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/data
    environment:
      - DASK_SCHEDULER
    command: tail -f /dev/null 
