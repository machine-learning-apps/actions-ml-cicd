name: Project Data to Pages
on:
  push:
    branches:
      - master
    paths:
      - 'project/model_card.md'
      - 'project/data_dictionary.md'
      - 'project/model_timeline.csv'

jobs:
  copy-files:
    runs-on: ubuntu-latest
    steps:
    - name: Copy Repository Contents
      uses: actions/checkout@master

    - uses: actions/setup-python@v1
      with:
        python-version: '3.6'
        architecture: 'x64'
    
      # Copy the files and append appropriate jekyll headers
    - name: move and modify files
      run: |
        chmod -R 777 docs/docs
        ./action_files/copy_files.sh

    - name: commit & push files
      run: ./action_files/push_to_repo.sh docs/model_card docs/model_timeline docs/data_dictionary
